/**
 * The file to define supported constants that will be
 * used in schema validation and also the key when
 * the supported hooks or implementation can be referenced.
 */
export const supportedEmailSenderSuffixes = ['muze.co.th']
